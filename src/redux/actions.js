import { TABLE_RESIZE } from './types';

// Тут храняться функции action creators; Создающие не
import { TABLE_RESIZE } from './types';
// Action creator
export function tableResize(data) {
  return {
    type: TABLE_RESIZE, // Alias
  };
}
